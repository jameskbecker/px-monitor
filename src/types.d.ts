export type latestPx = { 
    tag: string;
    fTag: string;
    appId: string;
    scriptUrl: string;
    site: string;
};

export type tags = {
    tag: string;
    fTag: string;
};

export type scriptData = {
    appId: string;
    scriptUrl: string;
}